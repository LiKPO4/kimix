# Kimix v2.11.27

## 修复

- Server 会话下使用 /goal、/swarm 或 /reload 时，不再误走 SDK-only 会话表，也不再显示“重载成功”这类伪成功提示。
- 当官方 Server 暂未公开对应能力时，Kimix 会给出明确的不支持提示，避免用户误以为已经原生对齐。

## 调整

- Slash 补全文案收敛为“SDK 兼容能力 / Server 暂不支持”，减少“官方”字样造成的误导。

