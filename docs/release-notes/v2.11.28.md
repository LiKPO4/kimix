# Kimix v2.11.28

## 修复

- 启动或切换项目时改用官方 Server 会话列表做目录对账；如果会话已在官方网页归档，Kimix 本地侧栏会同步隐藏。
- 只在官方 Server 列表成功返回时才执行“缺失即隐藏”，避免 SDK fallback、离线历史或本地-only 会话被误归档。

## 保护

- 长程任务会话、本地-only 会话和其他项目会话不会因为当前项目官方列表缺失而被隐藏。

