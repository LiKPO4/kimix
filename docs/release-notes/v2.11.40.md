# Kimix v2.11.40

## 修复

- 重新打开 Server 历史会话时，会补回仍在等待处理的工具审批卡。
- 重新打开 Server 历史会话时，会补回仍在等待回答的问题卡。
- 实时运行中的 Server snapshot 恢复逻辑保持不变，避免重复显示待处理项。

## 说明

- Server Terminal 不作为 Kimix 主聊天 UI 的必补对齐项，后续只在明确需要终端面板时再接前端入口。
