# Kimix v2.9.36

## 修复

- 修复画板 1:1 画布仍按自身尺寸撑开弹窗、遮挡底部操作区的问题。
- 画板右侧舞台改为测量可用空间后计算显示尺寸，背景层、绘制层、对象 SVG 层和裁剪框统一绑定到同一个固定显示框。
- 版本号三处同步到 v2.9.36：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
