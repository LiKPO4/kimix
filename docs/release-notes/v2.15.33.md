# Kimix v2.15.33

## 孤儿 Agent 会话找回

- 搜索弹窗新增“待找回 Agent”筛选，集中显示带 Kimix 房间来源、但当前没有绑定到本地协同房间的官方会话。
- 找回结果显示原房间 ID 和 Agent ID；元数据 schema 异常的会话也不会被隐藏。
- 点击“独立打开”会加载完整官方历史并保留为普通会话，不会猜测目标房间或自动改写任何 Agent 绑定。
- 已正确绑定到协同房间的 Agent 不会误入待找回列表；catalog 生成的独立镜像仍可被该入口发现。
