# Kimix v2.12.58

## 修复

- 启动恢复官方会话时不再先展示过期本地缓存的“伪底部”；首轮官方历史同步完成前显示明确的底部同步状态，随后一次性展示真实尾部。
- 移除启动恢复官方历史前人为等待的 1.2 秒，让会话目录、运行时和历史读取立即开始。
- Server snapshot 回放现在保留每条消息的官方 `created_at`；重启或重新加载历史不再把旧消息时间改成当前加载时间。
