# Kimix v2.11.30

## 修复

- 自动发送下一条排队消息前，会先核对官方 Server 的 active/queued prompt；官方队列未清空时保留本地待发消息，不再抢跑。
- 普通会话和长程任务使用相同的官方队列门禁。
- 停止 Server 轮次后会复查官方 prompt 队列；仍有任务时保持运行状态，不再误报已经完全停止。

## 兼容

- SDK 会话或官方队列查询失败时继续使用原本地队列逻辑，不阻断用户消息。
