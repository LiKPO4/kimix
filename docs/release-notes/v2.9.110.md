# Kimix v2.9.110

## Kimi Code 0.15.0 跟进

- MCP 服务配置新增官方 `sse` 传输方式。
- MCP 添加面板、IPC 类型、主进程校验、Plugin MCP 导入和配置读取都支持 `stdio | http | sse`。
- SSE MCP 与 HTTP MCP 一样填写 URL；stdio MCP 仍填写命令和参数。

## 验证

- `pnpm build`

