# Kimix v2.9.28

## 修复

- 根目录 `start-kimix.bat` 改为调用安全重启脚本，启动前会清理 Kimix dev 旧进程和 Vite/构建缓存，避免继续打开旧版本窗口。
- 版本号三处同步到 v2.9.28：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
