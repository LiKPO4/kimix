# Kimix v2.9.32

## 修复

- 修复连续绘制多个图形后，撤销到最后一个图形时需要重复撤销多次的问题。
- 图形对象固定到像素层时不再新增一条视觉重复的撤销记录，而是替换当前历史顶。
- 撤销/重做恢复快照时，仅在画布尺寸变化时重设 canvas 宽高，减少连续撤销时的整画布闪烁。
- 版本号三处同步到 v2.9.32：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
