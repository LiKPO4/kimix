# Kimix v2.9.42

## 跟进

- 跟进 Kimi Code 0.14.2 的 `tool.progress` 事件，Bash 等 shell 工具运行中会把 stdout/stderr 合并到同一个工具卡，展开即可查看实时输出。
- Slash 命令补齐 `/reload`、`/status`、`/usage`：分别重载当前 SDK 会话、显示当前会话状态、显示当前会话用量。
- 核查官方 0.14.2 slash 注册表：`/provider` 等 TUI 配置入口仍保留为 Kimix 设置页/模型配置入口，不新增伪聊天命令。
- 版本号三处同步到 v2.9.42：`package.json`、侧栏、设置页。

## 验证

- `pnpm test:run -- src/utils/__tests__/kimiCodeEventMapper.test.ts src/utils/__tests__/eventMapper.test.ts` 通过。
- `pnpm build` 通过。
