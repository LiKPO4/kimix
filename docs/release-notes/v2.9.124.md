# Kimix v2.9.124

## Kimi Code 0.16.0 跟进
- 接入官方新增的 `kimi vis` 会话可视化能力：会话菜单新增“会话可视化”，可直接打开当前 Kimi Code 官方 session 的浏览器可视化页面。
- `kimi:startVis` IPC 支持传入 `sessionId`，并默认交给 Kimi Code CLI 打开浏览器；保留 `noOpen` 参数用于后续无浏览器启动场景。
- 更新 `kimi vis` 启动失败提示，明确要求 Kimi Code CLI v0.16.0 或更高版本。

## 验证
- `pnpm build`
