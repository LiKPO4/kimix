# Kimix v2.14.32

## 修复

- 修复 Swarm 长时间运行时，Kimix 可能把仍有子代理/工具在执行的会话提前显示为“已完成”的问题。
- 修复提前放开输入后，再发送下一条消息触发官方 `Cannot launch a new turn while another turn is active` 错误的问题。

## 说明

- 历史残留运行态仍会按原防卡死规则清理；本次只阻止当前未闭合的子代理/工具被 runtime 状态轮询误判为完成。
