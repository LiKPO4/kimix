# Kimix v2.15.1

## 修复

- 修复会话持久化在写入失败后可能让旧排队快照覆盖新状态的问题。
- 修复 Intel Mac 无法识别旧版无 `x64` 后缀更新资产的问题；后续 macOS 构建产物统一带架构后缀。
- 将 Agent 运行耗时刷新从持续 `requestAnimationFrame` 改为按秒对齐调度，降低空闲渲染开销。

## 验证

- 新增持久化交错失败顺序和 macOS 更新资产选择回归测试。
- 由 GitHub Actions tag 流程构建和发布，不包含本地手动构建产物。
