# Kimix v2.9.148

## 改进

- 设置页新增“实验功能”模块，可直接开启官方 Kimi Code Server 和新会话 Server 路由，不再需要手动设置环境变量。
- 实验开关默认关闭，保存后需要完全重启 Kimix 生效；关闭 Server 时会自动关闭 Server 会话路由。

## 说明

- Server 会话路由仍是灰度能力，异常时保留 SDK 回滚路径。
- Windows terminal 与 Server OAuth 写链路仍受官方 0.17.1 当前行为限制，暂不作为正式入口。
