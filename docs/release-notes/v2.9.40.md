# Kimix v2.9.40

## 修复

- 画板画布显示尺寸增加垂直呼吸空间，避免上下边框被裁切。
- 选中图形时，对象属性卡提前到“线条宽度”后方，出现在左侧工具栏上半段。
- 版本号三处同步到 v2.9.40：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
