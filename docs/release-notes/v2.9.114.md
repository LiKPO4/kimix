# Kimix v2.9.114

## 搜索定位

- 修复点击搜索结果后，离开会话再回来仍会重复跳转和高亮的问题。
- assistant 多段回复合并渲染时，现在会保留每段原始事件 id，搜索命中可以定位到正确渲染块。
- 搜索文字选择支持跨 DOM 文本节点，提升 Markdown 正文、加粗、行内代码等内容的命中精度。

## 搜索加载

- 补充加载最近历史时，同一弹层周期内同一条空历史只尝试一次，避免“正在补充加载最近历史...”反复出现。

## 验证

- `pnpm build`
