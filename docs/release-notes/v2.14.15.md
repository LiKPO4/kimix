# v2.14.15

- 修复切换权限模式时 Assistant 底部模型 / Tokens / Context 元信息短暂闪烁、时间被刷新成权限切换时间的问题。
- 空闲状态下的 `agent.status.updated` 只视为 runtime 快照，不再写入会话消息流，因此不会污染上一条回复的 footer。
- 保留真正的 `usage.record` 和活跃轮次中的状态更新，官方能提供的 Tokens / Context 仍会正常显示。
