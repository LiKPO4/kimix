# Kimix v2.11.34

## 修复

- Server 会话中的项目文本预览优先使用官方会话级文件读取接口。
- 预览仍限制为 UTF-8 文本且不超过 1 MiB，并校验会话工作目录与项目一致。
- 对话中的 Plan 预览和右侧文件预览都会携带当前运行时会话。

## 兼容

- SDK 会话、官方读取失败、二进制或超限文件继续使用原本地校验流程。
- Kimi 用户目录中的 Plan 文件仍由本地路径读取。
