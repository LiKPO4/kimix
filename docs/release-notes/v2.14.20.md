# Kimix v2.14.20

## 调整

- Kimi Web 过程里的不可展开正文句改为与普通正文一致的 14.5px 字号和 24px 行高。
- 可展开的 Kimi Web 过程正文也保持同级字号；辅助计数、状态图标等仍保留较小辅助层级。

## 验收建议

- 截图中类似 `Now create...` / `forest.json doesn't exist...` 的过程句应与普通正文视觉字号一致。
- 不可展开过程句仍不显示 hover 背景、不响应点击，但可以正常选中文字复制。
