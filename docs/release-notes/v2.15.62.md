# Kimix v2.15.62

## 重启后自动发现残留 Agent 任务

- 运行态校准现在会直接扫描房间持久化的 active delivery，不再依赖重启后已经丢失的内存 activity 或 `runningSessionId`。
- 持久化任务本身会唤醒校准循环，因此打开 Kimix 后无需再次点击停止即可检查对应 Agent runtime。
- runtime 确认不活跃或不存在时，继续沿用 v2.15.61 的 Agent 级安全收尾，不恢复或重发消息。
- 瞬时网络错误仍保留运行态，避免把暂时无法查询误判为任务终止。
