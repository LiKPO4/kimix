# Kimix v2.15.51

## 稳定多 Agent 消息信息气泡

- 每个 Agent 回复只在自己的 `agentTurnId` 内选择最终状态，晚到的权限、Plan 或运行状态不再覆盖已有 Tokens/Context。
- 另一个 Agent 开始输出时，已完成回复继续显示其自身的最终用量。
- 多 Agent 回复缺少最终指标时，优先显示该回复官方事件记录的模型；没有模型时只显示“已完成”，不再把历史回放间隔误当成生成耗时。
- 本次仅调整渲染选择与显示降级，不改写官方事件、历史记录或消息归属。
