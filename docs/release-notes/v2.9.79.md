# Kimix v2.9.79

- 跟进 Kimi Code 0.14.2：补充配置诊断、插件/Skill 展示、标题读取与会话恢复细节。
- 修复轮次达到推荐上限后的交接新会话流程，避免“交接中”卡死；重启后会清理陈旧交接运行态，允许再次点击。
- 优化长思考和长会话渲染：计时刷新不再带动整块消息重渲染，过程详情默认只挂载最近 120 条，较早内容可手动展开。
- 优化关闭窗口时的本地会话保存：运行中会按最长等待兜底周期保存，关闭时避免同步序列化大历史造成界面卡顿。
- 修复旧会话图片恢复、附件路径、TodoList 清空、上下文压缩/恢复状态、Swarm 子代理展示等多处会话流问题。
- 统一工具审批、按钮 hover、状态胶囊、消息尾部信息、右侧留白等 UI 细节。
