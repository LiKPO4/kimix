# Kimix v2.11.18

- 修复输入 `/skill:` 时只显示命令本身、无法预览已有 Skill 的问题。
- `/skill:` 候选面板现在会展示官方会话 Skill 和本地已安装 Skill，支持按名称/描述过滤，并可点选填入 `/skill:<name>`。
- Skill 候选使用独立标题和图标，避免与普通 slash 命令混在一起。
- 修复选中 Skill 后命令仍被当作普通消息发送的问题；`/skill:<name>` 现在会在发送 prompt 前直接调用官方 Skill 激活接口。
