# Kimix v2.9.105

## 新增

- 将右侧“差异面板”改为“文件预览”，打开后列出当前项目根目录下允许预览的文本文件。
- 文件预览支持点击根目录文件后直接预览内容，Markdown 文件使用聊天内同款 Markdown 渲染，其他文本文件使用等宽纯文本预览。
- 设置页新增“文件预览”区块，可配置允许预览的扩展名，默认 `md, txt`。

## 说明

- 文件列表只扫描项目根目录第一层，超过 1 MB 的文件会跳过，避免大项目打开右栏时卡顿。

## 验证

- `pnpm test:run`
- `pnpm build`
