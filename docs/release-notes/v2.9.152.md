# Kimix v2.9.152

## 优化

- 新建或切换到空白 Kimi Code 会话后，Kimix 会在后台预热 runtime。
- 目标是把原本首条消息前的 create/resume 握手耗时前移到用户输入前，让首条消息尽量也走接近第二条消息的热路径。

## 兼容性

- 预热只针对无消息、未运行、无 runtime 的空白 Kimi Code 会话。
- 预热失败不会弹红错，也不会阻止用户发送；实际发送时仍走原有创建与 fallback 链路。
