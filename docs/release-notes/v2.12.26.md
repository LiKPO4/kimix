# Kimix v2.12.26

## 修复

- 模型弹窗只允许切换 Kimi Code 配置中已存在的模型别名；Server 模型目录仅用于补充名称和上下文信息。
- 切换已配置的第三方 OpenAI 兼容模型时，如模型缺少 `max_output_size`，Kimix 会通过官方配置接口补入保守的 65536 上限，避免超大上下文模型生成越界的 `max_tokens`。
- 切换模型后产生的空闲模型状态不再覆盖上一轮 Assistant 消息的模型信息；新模型从下一条用户消息开始归属。
