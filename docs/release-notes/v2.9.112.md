# Kimix v2.9.112

## 搜索与恢复命令

- 搜索面板补充历史加载完成后会显示短暂结果提示，不再长期停留在“正在补充加载最近历史...”。
- 当前项目搜索结果点击后会打开对应会话，并滚动、高亮到命中的消息块。
- 如果命中内容位于折叠的早期会话里，会先自动展开折叠历史再定位。
- 全工作目录视图复制的恢复命令改为 PowerShell 兼容写法。

## 验证

- `pnpm build`
