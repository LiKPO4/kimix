# Kimix v2.9.33

## 修复

- 画板裁剪提示卡移动到“线条宽度”和“工具”之间，靠近用户需要注意的位置。
- 支持按 Enter 直接应用裁剪，按 Esc 取消裁剪选区。
- 固定画板主体高度，并把滚动限制在左侧工具栏内部，避免工具栏出现滚动条时改变右侧绘图页面高度。
- 版本号三处同步到 v2.9.33：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
