# Kimix v2.9.48

## 修复

- 统一高优先级按钮 hover 反馈：停止按钮、弹窗强按钮、审批允许按钮不再用透明度变淡作为 hover。
- 修复画板删除对象、附件删除、MCP OAuth 开关、顶部工具栏组合按钮的 hover 色体系不一致问题。
- 版本号三处同步到 v2.9.48：`package.json`、侧栏、设置页。

## 验证

- `pnpm build`
- `git diff --check`
