# Kimix v2.9.41

## 跟进

- 更新 vendored Kimi Code Node SDK 到官方 0.14.2 对应的 `@moonshot-ai/kimi-code-sdk` 0.9.3。
- BTW 侧问改为优先使用官方 `withInteractiveAgent()` 作用域 API，避免并发/子代理场景下交互代理串线；旧 SDK 仍保留兼容回退。
- 补充 SDK provenance，记录 0.14.2 source commit、node-sdk 版本和本地 CLI 验证版本。
- 版本号三处同步到 v2.9.41：`package.json`、侧栏、设置页。

## 验证

- 待本轮执行。
