# Kimix v2.11.31

## 修复

- Slash 补全现在会根据当前会话实际使用的 Server 或 SDK 运行时动态生成。
- Server 会话不再显示尚无官方接口支持的 `/goal`、`/swarm` 和 `/reload`。
- SDK 会话继续保留上述兼容能力；运行时尚未恢复或查询失败时使用保守清单，避免展示不可用操作。

## 验证

- 新增 Server/SDK Slash 清单边界测试，并覆盖现有 Slash 路由行为。
