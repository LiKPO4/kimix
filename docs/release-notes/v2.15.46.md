# Kimix v2.15.46

## 稳定多 Agent 工具栏宽度

- 修复底部权限按钮外层被压缩后，按钮本体仍按内容宽度向右溢出并侵入 Agent 选择器的问题。
- 权限按钮现在使用固定 124px 槽位，内部文字在固定边界内省略；Agent 名称继续在自己的可用宽度内自适应缩小。
- 权限、Agent 和正文范围按钮之间使用明确的 8px 间距，避免 hover 背景和相邻热区视觉相连。
