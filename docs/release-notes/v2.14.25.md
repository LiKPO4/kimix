# Kimix v2.14.25

## 修复

- 继续收口刚启动时 Skill fork 同名会话重复显示的问题。
- 当两条本地镜像没有共享 runtime/official 身份，但同项目、同标题，且至少一条带 `skill-*` 身份时，侧栏会把它们视为同一个透明 Skill fork 镜像，只展示一条。

## 验收建议

- 刚打开 Project06 时，“不对，我是说之前...”应直接只显示一个。
- 普通同项目同名会话不应仅因为标题相同就被合并。
