# Kimix v2.15.55

## 修复多 Agent 房间重复显示用户消息

- 修复房间消息在运行中可能短暂显示多份、重启后仍保留两份的问题。
- 当发送接口没有返回官方用户事件 ID 时，只在同一 Agent 的官方历史中存在唯一的相同正文、且时间相差不超过 30 秒时绑定房间投递。
- 连续发送相同文字导致多个候选时不会猜测归属，避免合并真实的重复输入或搅乱 Agent 消息边界。
- 提升历史缓存版本，使已有房间在重新加载后也会重新归一化；不会修改 Kimi Code 的官方会话文件。
