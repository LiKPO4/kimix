# Kimix v2.11.36

## 修复

- Server 会话中的本地图片改为先通过官方 `/files` 接口上传，再使用返回的文件 ID 发送。
- 普通消息、插话和 BTW 侧问共用同一套官方图片上传链路。
- multipart 上传不再错误携带 JSON Content-Type。

## 兼容性

- 网络图片继续使用官方 URL 图片源。
- SDK 会话的图片输入方式保持不变。
