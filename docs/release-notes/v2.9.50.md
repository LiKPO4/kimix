# Kimix v2.9.50

## 修复

- 工具审批卡片统一为 Kimix 面板软边框和轻阴影，去掉突兀的深色外框。
- 修复流式输出把 `## 标题` 拼在上一句后面时，Markdown 标题无法正常渲染的问题。

## 验证

- `pnpm test:run -- src/utils/__tests__/assistantParagraphs.test.ts`
- `pnpm build`
- `git diff --check`
