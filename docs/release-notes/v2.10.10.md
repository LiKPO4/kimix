# Kimix v2.10.10

## SDK 首次响应超时优化

- 根据 Kimi Code 会话日志定位到固定 30 秒等待来自不可用 MCP 服务的默认启动超时，而非模型思考。
- 将未单独配置的 MCP 启动上限从 30 秒收紧为 4 秒，故障服务会快速降级并让模型继续响应。
- 显式声明 `startupTimeoutMs` 的 MCP 仍使用自身配置；可通过环境变量覆盖 Kimix 默认值。
- vendor SDK 刷新脚本会自动重放该策略，避免后续升级官方 SDK 时静默丢失。
