# Kimix v2.15.61

## 收束被外部终止的 Agent 运行态

- 当 Agent 进程因应用重启、崩溃或系统终止而消失时，官方返回“不活跃”或“不存在”会被识别为确定终态。
- 运行态校准会自动关闭该 Agent 的未完成事件、投递状态和活动标记，不再永久停留在“运行中”。
- 用户点击停止时，如果目标 runtime 已经消失，也会安全完成本地状态收尾，而不是反复显示停止失败。
- 收尾严格限定到对应 Agent 和投递，不恢复、不重发消息，也不影响房间内其他 Agent。
