# Kimix v2.11.57

## 跟进 Kimi Code 0.20.0

- 刷新内置 Kimi Code SDK 到官方 `@moonshot-ai/kimi-code@0.20.0` 对应源码，保留 Kimix MCP 启动超时兜底补丁。
- 对话 Markdown 支持渲染 `$$...$$` 行间 LaTeX 公式，并为长公式保留横向滚动和基础留白。
- 兼容链路中用户显式执行 `/reload` 时，会请求官方 SDK 刷新插件 Skill 视图。
- Server 会话的 `/reload` 仍按官方公开能力保持边界：当前没有确认可用的 Server REST reload 路由，因此继续提示新建或 fork 会话来刷新配置。

## 后续评估

- 官方 0.20.0 的 shell 模式、`kimi web --host`、插件页重设计、web 文件逐行 diff、会话分页和标题同步属于更大的产品面调整，本版先完成主聊天链路可落地项。
