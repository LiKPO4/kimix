# Kimix v2.9.27

## 改进

- 画板新增“选择”工具，基础图形绘制后可再次选中。
- 方形、圆角矩形、圆形、线条改为对象层绘制，支持移动、缩放、旋转和删除。
- 保存/复制画板时会把背景、像素笔刷和对象图形合成为同一张 PNG。
- 选中对象后左侧显示对象属性卡，提示移动、缩放、旋转操作。
- 版本号三处同步到 v2.9.27：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
