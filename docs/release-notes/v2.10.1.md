# Kimix v2.10.1

## 修复

- 修复 v2.10.0 启动后恢复上次 Kimi Code Server 会话时，旧 runtime 已被官方 Server 清理但 Kimix 仍尝试写入 `/profile`，导致对话中出现“Session was not found”红色错误卡的问题。
- 旧 Server session 丢失时改为自动创建同项目 fresh runtime，保留 Kimix 本地聊天记录，不再把单个 session missing 误判为 Server 整体不可用。

## 验证

- `git diff --check`
- `pnpm test:run`
- `pnpm build`
