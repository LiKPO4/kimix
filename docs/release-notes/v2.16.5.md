# Kimix v2.16.5

本版本修复长思考切换为最终正文时，手动浏览中的会话视口向上跳动。

## 修复

- 最终正文首次出现时保留思考视口的过渡帧几何，再将过程详情作为一次受保护的布局事务收起，避免先变为完成态摘要、再二次折叠。
- 手动浏览时采样当前视口内仍存活的内容锚点；若内容收缩使浏览器先压低滚动上限，则在聊天流尾部建立临时补偿，并随最终正文增长自动消化。
- 只有滚轮、触摸、滚动条拖动和导航键等明确输入才会推进用户滚动代次；浏览器高度钳制与程序恢复不再污染用户意图或反复启用旧锚点。
- 自动跟随、多 Agent 消息归属、思考历史和最终正文数据均保持不变。
