# Kimix v2.13.2

## 修复

- 修复迁移会话的 Server snapshot 为空时一直加载的问题：失败、超时或空 snapshot 会自动回退到本地 SDK wire 历史。
- 修复从全局搜索直接打开会话时只显示加载占位、没有真正读取历史的问题。
- 历史读取失败时结束加载状态并显示错误，不再无限转圈。
- 归档同一官方会话的所有本地镜像，并立即持久化归档 tombstone，避免重启后重新出现。
