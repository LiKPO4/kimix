# Kimix v2.9.29

## 修复

- 修复重新构建/启动恢复后，已归档对话可能被官方 Kimi Code 历史重新拉回侧栏的问题。
- 归档对话现在会额外记录稳定 tombstone，并按官方 session id、runtime session id、长程任务子会话 id 过滤启动恢复结果。
- 启动恢复出来的会话会写入 `officialSessionId`，避免后续无法与本地归档状态匹配。
- 版本号三处同步到 v2.9.29：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
