# Kimix v2.15.43

## 多 Agent 房间独立身份协议

- 每次房间投递都会向模型声明当前接收者身份，并明确房间成员是拥有独立上下文和会话的协作者，不是当前 Agent 扮演的多个角色。
- 桥接历史逐条区分“用户消息”和“其他独立 Agent 消息”，同时标注真实发言者；引用同伴结论时必须按 Agent 名称归因。
- 即使本轮选择“不补充”，当前 Agent 仍会收到简短身份协议；协议继续通过隐藏信封传输，不会出现在对话历史、搜索或导出正文中。
