# Kimix v2.14.33

## 改进

- 用户消息下方操作按钮更紧凑；失败发送的用户消息会显示“删除本地消息”按钮。
- 删除失败发送的用户消息时，会同步清理其本地发送状态、空 assistant 占位和错误卡。
- 错误卡右上角关闭现在会从会话事件里移除该错误，不再只是当前渲染临时隐藏。

## 边界

- 删除只影响 Kimix 本地显示记录，不会回滚已经提交给官方 runtime 的远端上下文。
