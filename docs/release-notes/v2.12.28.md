# Kimix v2.12.28

## 修复

- 历史消息的模型信息改用官方逐轮 `usage.record`，不再把后续 `config.update` 或空闲状态误贴到上一轮回复。
- 会话模型读取改为使用 wire 中最后一条有效模型记录，不再返回文件开头的初始模型。
- Server 请求失败迁移到 SDK runtime 后，重新读取官方状态并同步底栏模型，避免底栏继续显示迁移前的选择。
