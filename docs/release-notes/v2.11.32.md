# Kimix v2.11.32

## 修复

- 新建 Kimi Server 会话前会先通过官方接口注册或刷新对应工作区。
- 会话创建改为携带官方 `workspace_id`，并使用 Server 返回的规范工作目录。
- Kimix 的项目置顶、排序、Git 和长程任务数据继续保留在本地，不会被官方 Workspace 覆盖。

## 兼容

- 官方 Workspace 注册或 Server 会话创建失败时，继续使用现有 SDK 回退链路。
