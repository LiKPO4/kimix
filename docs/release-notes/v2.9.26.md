# Kimix v2.9.26

## 改进

- 优化画板左侧工具区布局：从画笔工具开始改为两列排列，减少纵向拥挤。
- 调整画板颜色/背景切换与色板之间的留白，使用明确间距避免视觉贴合。
- 版本号三处同步到 v2.9.26：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
