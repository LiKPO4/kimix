# Kimix v2.9.31

## 修复

- 修复滚动条出现/消失时挤压布局，导致主界面宽度和画板左侧工具栏发生横向重排的问题。
- 左侧项目/会话列表、对话流滚动区、画板工具栏现在保留稳定滚动槽，避免绘制对象或内容增多时页面抖动。
- 版本号三处同步到 v2.9.31：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
