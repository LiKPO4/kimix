# Kimix v2.9.25

## 主要更新

- 调整 Windows NSIS 安装器升级流程：升级前预写 `KeepShortcuts` 标记，尽量让旧卸载器保留桌面和开始菜单快捷方式，避免更新后删除旧桌面图标再创建新图标。
- 版本号三处同步到 v2.9.25：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过，renderer hash：`assets/index-Br18OH7A.js`。
- `pnpm test:run` 通过：10 个测试文件、115 个测试全部通过。
- `git diff --check` 通过；仅提示 Windows LF/CRLF warning。
