# Kimix v2.9.45

## 修复与跟进

- 接入官方 Kimi Code SDK `getConfigDiagnostics()`，在插件 / Skills 页展示配置警告；无警告时显示正常状态。
- 官方 SDK Skills 展示补齐 Sub-skill 标识与数量统计，便于区分普通 Skill 与 0.14.2 新增的子技能。
- 历史回放兼容 SDK 原生 `assistant.delta`、`thinking.delta`、`tool.progress`、`turn.ended` 和 `compaction.*` 事件，避免上下文压缩 replay 后丢失后续输出或工具进度。
- 版本号三处同步到 v2.9.45：`package.json`、侧栏、设置页。

## 验证

- `pnpm test:run -- src/utils/__tests__/eventMapper.test.ts src/utils/__tests__/kimiCodeEventMapper.test.ts src/utils/__tests__/kimiCodeSendRetry.test.ts`
- `pnpm build`
