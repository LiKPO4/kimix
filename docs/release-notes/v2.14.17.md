# Kimix v2.14.17

## 修复

- 修复用户滚轮操作后，再切换权限模式会把聊天流瞬间顶到顶部的问题。
- 用户手动滚动后会在空闲帧持续捕获当前可见消息锚点；权限切换期间会在事件帧、后续动画帧和内容版本变化后恢复该锚点，避免权限菜单关闭、运行时状态快照或 Markdown 延迟布局把滚动容器复位到顶部。

## 验收建议

- 进入长会话后不滚动，反复切换权限，确认不跳顶。
- 用滚轮上下滚动后，再切换 manual / auto / yolo，确认仍停留在当前可见位置，不会闪到顶部。
- 在当前轮运行中切换权限，确认仍显示延后切换提示，且当前轮结束后权限应用。
