# Kimix v2.15.63

## 修正多 Agent 本轮耗时锚点

- 房间 Assistant 的本轮耗时只匹配相同 `roomMessageId` 的用户消息，不再回退到该 Agent 的任意旧用户消息。
- 当前 prompt 尚未进入 Agent 事件分区时，使用本轮 Assistant 占位起点计算，不跨回合猜测。
- 普通单 Agent 会话继续使用最近用户消息作为完整回合起点，既有耗时语义不变。
- 修复刚完成的约 10 秒回复偶尔显示成四十多分钟的问题。
