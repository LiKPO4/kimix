# Kimix v2.9.47

## 修复

- 修复 Kimi Code 实时输出文字片段重复的问题：旧兼容事件通道 `kimi:event` 不再处理 Kimi Code/长程任务会话，避免与 `kimi-code:event` 新通道同时合并同一批 SDK 原生 delta。
- 版本号三处同步到 v2.9.47：`package.json`、侧栏、设置页。

## 验证

- `pnpm test:run -- src/utils/__tests__/eventMapper.test.ts src/utils/__tests__/kimiCodeEventMapper.test.ts src/utils/__tests__/kimiCodeSendRetry.test.ts`
- `pnpm build`
