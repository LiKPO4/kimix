# Kimix v2.15.22

## 改进

- 完善协同房间的逐 Agent 归档与恢复事务，一个 Agent 失败时不再错误隐藏整个房间。
- 部分成功会保留逐 Agent 错误和可重试状态，恢复任一 Agent 后房间立即重新可见。
- 次要 Agent 移出房间时保留完整历史，并把原官方会话安全转换为独立对话。

## 兼容性

- 普通单 Agent 会话的归档与恢复流程保持不变。
- 协同房间用户入口仍处于开发门禁内，不会在现有会话中提前显示未完成入口。
