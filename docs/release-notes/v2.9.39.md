# Kimix v2.9.39

## 修复

- 修复画板右侧出现额外浅灰层、画布没有充分利用右侧空间的问题。
- 画板弹窗改为稳定高度布局，中间区域直接吃满剩余空间。
- 左侧工具栏固定在中间区域内滚动，避免对象属性卡把左栏和底部操作区顶爆。
- 版本号三处同步到 v2.9.39：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
