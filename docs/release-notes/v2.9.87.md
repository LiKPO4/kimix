# Kimix v2.9.87

- 停止 dev 版与安装版之间的自动会话共享，避免后台同步和启动/聚焦流程再次引入卡顿风险。
- 设置页新增“会话迁移”：支持导出全部 Kimix 会话快照，并以去重合并方式导入到另一端。
- 导入时按 UI 会话 id、官方 session id、runtime session id 去重合并，不覆盖本地已有且更新的内容。
- 快照包含会话、待发送队列、项目列表、归档 tombstone 和隐藏交接记录；导入到另一端后，已归档会话仍保持归档状态。
