# Kimix v2.9.30

## 修复

- 画板撤销/重做升级为完整快照，覆盖背景层、笔刷层、对象层和画布尺寸。
- 修复图形对象新增、移动、缩放、旋转、删除无法撤销/重做的问题。
- 修复裁剪后撤销无法正确恢复画布宽高的问题。
- 图形对象完成调整后，切换到其他工具、按 Esc 或点击空白会固定到像素层；固定后可用油漆桶对其围出的区域上色。
- 版本号三处同步到 v2.9.30：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
