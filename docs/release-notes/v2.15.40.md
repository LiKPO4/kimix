# Kimix v2.15.40

## 多 Agent 房间投递状态与工具栏修正

- 新消息的可靠持久化阶段不再误显示成“等待当前任务结束”；只有确实被同一 Agent 的前序工作阻塞时才展示排队卡。
- 房间底栏和停止按钮只把真实发送、运行或等待交互视为 Agent 正在执行，不再把内部 `queued` 状态显示成“运行中”。
- 输入区恢复“权限在前、Agent 在后”的工具顺序，保持原有权限入口位置。
- Agent 选择器固定为 34px 高，并校正名称与接收者数量的行高和垂直对齐。
