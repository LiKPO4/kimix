# Kimix v2.9.138

- 修复 assistant 输出完整 Markdown 文件时，外层 ```markdown 内嵌 ```bash 导致内容被拆成多个渲染块的问题。
- 渲染前会把包含嵌套 backtick fence 的外层 markdown 代码围栏升级为更长围栏，避免内层命令块提前关闭外层。
- 表格和标题修复逻辑改为按围栏符号与长度判断 fenced code block 边界，避免误处理嵌套围栏内部内容。
