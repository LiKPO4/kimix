# Kimix v2.15.59

## 消除房间实时快照的用户消息别名

- 房间用户消息合并优先使用 `roomMessageId + agentTurnId + dispatchAttemptId`，同一投递即使快照延迟超过 10 秒也不会追加新气泡。
- 两次正文完全相同但稳定身份不同的真实发送会分别保留，不再被旧的正文与时间规则误合并。
- 房间投影会认领同一稳定投递的全部官方用户事件别名，已有运行时重复事件也只显示一个房间用户气泡。
- 普通单 Agent 会话及无稳定身份的旧历史继续使用原有的短时官方回声兼容逻辑。
