# Kimix v2.15.30

## 多 Agent 房间搜索归属

- 当前项目内容搜索会遍历房间内每个 Agent 的独立事件分区，次要 Agent 的回复、思考、工具和状态不再漏搜。
- 搜索结果显示来源 Agent 和模型；点击结果会先选择该 Agent，再滚动并高亮精确事件。
- 房间级标题和最近对话结果显示参与 Agent 数量，不伪造单个 Agent 所有权。
- “全部工作目录”中的次要 Agent 官方 session 会标注 Agent/模型，点击后打开现有房间和对应 Agent，不再创建重复独立会话。
