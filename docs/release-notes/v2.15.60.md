# Kimix v2.15.60

## 分 Agent 展示背景信息窗口

- 多 Agent 房间的背景信息窗口逐项展示每个 Agent 对应模型的独立上下文用量，不再只读取 primary 兼容镜像。
- 每项分别显示已用比例、已用标记数和模型自己的上下文上限。
- 尚未收到 Context 指标的 Agent 显示“等待上下文数据”，不再将数据缺失误报为 0% 已用。
- 圆环和“压缩”操作继续绑定 primary Agent，未改变既有 runtime 控制边界。
