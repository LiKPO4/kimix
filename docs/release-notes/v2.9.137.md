# Kimix v2.9.137

- 修复 steer 引导时上一轮 assistant 正文仍在补全代码围栏，却被切到第二个消息头下面的问题。
- steer 后如果新到达的 assistant 内容是在补全未闭合的 fenced code block 或 inline code，会继续归并回上一轮正文。
- 对 steer 后迟到的快照式正文做重叠去重，避免 SDK 重发前缀时出现重复内容。
