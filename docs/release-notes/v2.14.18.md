# Kimix v2.14.18

## 修复

- 修正 v2.14.17 手动滚动锚点恢复没有真正生效的问题。
- 普通 ResizeObserver 锚点恢复仍限制 300px，避免布局抖动误拉动视口；权限切换后的手动滚动恢复允许大位移，专门处理滚动容器被复位到顶部这类 300px 以上跳变。

## 诊断

- `restoreManualScrollAnchor` 现在记录恢复位移 `delta`、锚点偏移、是否超过普通 300px 阈值、是否找到目标节点，避免再次出现“日志显示 restored=true 但 scrollTop 没变”的假阳性。
