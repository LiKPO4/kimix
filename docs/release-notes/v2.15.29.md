# Kimix v2.15.29

## 多 Agent 通知与点击定位

- 房间 Agent 完成任务、等待提问或请求工具审批时，桌面通知会明确显示对应 Agent 名称。
- 即使关闭“通知显示内容”，非敏感提示正文仍保留 Agent 名称，不会退回无法区分来源的通用文案。
- 通知携带房间、Agent、turn 和精确事件身份；点击后自动打开对应项目和房间、选择该 Agent，并滚动高亮对应回答、提问或审批卡片。
- 次要 Agent runtime ID 现在也能反向解析到所属房间，避免通知点击后找不到本地会话。
