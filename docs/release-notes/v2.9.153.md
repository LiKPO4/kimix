# Kimix v2.9.153

## 修复

- 修复开启 Kimi Server 路由后，恢复旧 Kimi Code 会话可能报 `/profile: Session was not found` 的问题。
- 启动恢复遇到旧 runtime ID 不存在时，会改为创建同项目 fresh runtime，不再把可恢复错误写入聊天流红框。

## 优化

- 当前选中的空闲历史会话也会后台验证/预热 runtime。
- 如果旧 runtime 已失效，会提前替换为新的 runtime，减少老非 Server 会话首条发送时的前置等待。
