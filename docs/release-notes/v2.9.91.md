# Kimix v2.9.91

## 修复

- 修复恢复后的大型会话状态无法通过 localStorage 水合到界面的问题，启动时直接使用主进程会话快照恢复列表。
- 会话持久化改为按变化会话片段合并到主状态文件，避免进入新会话后延迟触发全量 70MB+ 状态序列化。
- 大型会话列表不再写入 localStorage，避免容量超限导致恢复会话无法显示。

## 验证

- `git diff --check`
- `pnpm build`
