# Kimix v2.9.93

## 修复与清理

- 展开“已折叠较早对话”时保持当前可视位置，不再因为插入早期消息而跳到会话顶部。
- 清理前序消息同步排查遗留的 LevelDB/SST 调试脚本、大体积恢复 JSON 和临时构建目录。
- 保留 v2.9.90 的 Markdown 导出路径收敛，以及 v2.9.89 的会话快照分叉副本保护。

## 验证

- `git diff --check`
- `pnpm build`
