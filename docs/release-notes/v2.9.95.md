# Kimix v2.9.95

## 修复

- 修复多个会话同时存在未完成输出时，侧栏只给最新运行会话显示加载转圈的问题。
- 侧栏会话运行态现在同时参考 UI 会话 id、runtime 会话 id、会话 loading 状态，以及未完成的 assistant、tool call、steer、subagent 事件。

## 验证

- `git diff --check`
- `pnpm build`
