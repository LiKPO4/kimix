# Kimix v2.10.9

## SDK 老会话首次响应优化

- 从 Kimi Code 官方 wire 时间线拆分发送、模型步骤开始与首 Token 时间，确认长等待主要发生在模型输出之前。
- 恢复 SDK 会话时读取并缓存当前模型、思考、权限和 Plan 配置。
- 对 SDK 与 Server 会话的配置更新增加同值短路，避免恢复、预热和发送链路反复写入未变化的配置。
- 保留真实配置变更能力；用户实际切换模型、思考、权限或 Plan 时仍会正常更新。
