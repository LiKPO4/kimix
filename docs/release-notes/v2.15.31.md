# Kimix v2.15.31

## 多 Agent 房间导出

- Markdown 导出覆盖整个协同房间，并在用户消息中标注实际接收者，在回复、工具和状态中标注来源 Agent。
- 导出文件包含 Agent、mention、Provider、模型和参与状态清单，交叉实施与审查记录可以脱离应用阅读。
- 导出 Kimi 调试包时先选择具体 Agent，再导出该 Agent 对应的官方会话；普通单 Agent 会话保持原有流程。
- 房间没有可用官方会话时会明确提示，不会静默回退到主 Agent。
