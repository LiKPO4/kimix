# Kimix v2.9.108

## 修复

- 当主输出事件已结束但子代理或工具仍在运行时，过程摘要不再显示“输出完成”。
- 子代理仍未结束时，会话仍保持运行态，摘要标题显示“子代理运行中”或“命令运行中”，避免误导用户以为本轮已经结束。

## 验证

- `pnpm test:run src/utils/__tests__/eventMapper.test.ts`
- `pnpm test:run`
- `pnpm build`
