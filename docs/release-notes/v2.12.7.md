# Kimix v2.12.7

- 完成前端规整 TodoList 第二轮：动态时长、Token、百分比、进度和 diff 计数统一使用等宽数字，减少内容更新时的横向跳动。
- 工具栏与图标动作统一 `scale(0.96)` 按压反馈，并将常见 32px 图标按钮归并到共享动作样式。
- 消息与代码块的复制成功图标改为共享 CSS cross-fade，在不增加依赖的情况下平滑切换复制状态。
