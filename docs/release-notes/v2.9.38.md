# Kimix v2.9.38

## 修复

- 修复画板显示区域被压缩成细条、画布不可见的问题。
- 画板显示尺寸不再通过自身内容反向测量，改为根据窗口和弹窗可用空间计算，避免高度塌缩循环。
- 版本号三处同步到 v2.9.38：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
