# Kimix v2.9.43

## 修复

- 修复上下文压缩完成后，当前轮次仍在执行但输入框提前解锁的问题；现在只以官方 SDK `turn.ended` / status completed 作为放行依据。
- 修复压缩边界后旧的运行中工具卡未收口，导致过程摘要持续显示“正在思考/执行中”的问题；真正 turn 结束时会兜底关闭仍 running 的工具卡。
- 最新用户轮状态判断改为只看最后一个 assistant 分段，避免同一轮里早先完成的分段掩盖后续未完成分段。
- 版本号三处同步到 v2.9.43：`package.json`、侧栏、设置页。

## 验证

- `pnpm test:run -- src/utils/__tests__/kimiCodeEventMapper.test.ts src/utils/__tests__/eventMapper.test.ts` 通过。
- `pnpm build` 通过。
