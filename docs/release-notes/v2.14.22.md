# Kimix v2.14.22

## 修复

- 修复 Skill 注册表刷新产生的 `skill-*` 透明 fork 在官方 metadata 丢失时，侧栏可能出现两个同名会话的问题。
- 修复点击这类会话后可能一直停在“正在同步最新会话”的问题：当 `skill-*` 历史为空时，会回退读取原父会话历史。

## 后续

- 该修复仍保留当前 Skill 透明 fork 兼容方案。后续需要重新审视官方新版 Skill/Server 能力，评估是否可以取消或简化 fork 刷新链路。
