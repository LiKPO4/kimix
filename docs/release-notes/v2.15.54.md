# Kimix v2.15.54

## 稳定 Kimi Web 长思考区域

- 当前仍在生成的最后一段思考最多自然增长到 6 行，超出后保持固定高度并在区域内部滚动，避免聊天内容持续上下位移。
- 思考位于底部时会继续跟随最新内容；手动向上查看后暂停跟随，回到底部附近自动恢复。
- 内部滚动与外层聊天滚动按边界自然衔接，并使用低对比度的 6px 滚动条。
- 最终正文开始输出时自动收起本轮过程，由最终结果接替主阅读区域；思考记录仍可重新展开查看和复制。
- 多 Agent 房间按每个 Agent 的活跃状态独立判断，不改变消息归属、历史记录或投递逻辑。
