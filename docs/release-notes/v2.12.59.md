# Kimix v2.12.59

## 修复

- 本地会话恢复进入 store 时立即将活动官方会话标记为同步中，关闭 bootstrap 前旧缓存尾部短暂可见的窗口。
- 后台历史修复不再并行改写当前活动会话；启动主恢复成为活动会话历史的唯一写入者，避免真实尾部及消息元信息二次闪烁。
- SDK wire 的 `{ message: ... }` 包装记录保留消息或外层记录的原始时间，不再在第二次历史映射时回退为当前加载时间。
