# Kimix v2.15.35

## 多 Agent 房间内部验收入口

- 设置“实验功能”新增“多 Agent 房间（内部验收）”，无需开发者工具即可在本机开放 Composer `+` 菜单中的“添加 Agent”。
- 开关默认关闭且只保存在当前设备；关闭后普通会话立即隐藏创建入口，已有房间与历史不会删除。
- 新增四 Agent 端到端数据验收场景，覆盖同 Provider 不同模型、跨 Provider、长名称、用户选择顺序和单 Provider 失败隔离。
- 一个 Agent 运行或失败时，其他空闲 Agent 仍按各自队列继续调度，展示顺序保持用户冻结的接收者顺序。
