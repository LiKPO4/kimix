# Kimix v2.9.23

## 主要更新

- 修复拖拽普通文件或文件夹到输入框后附件卡片显示“未读取到绝对路径”的问题；Electron 桌面端改用 `webUtils.getPathForFile()` 读取真实路径，并保留旧 `File.path` fallback。
- 版本号三处同步到 v2.9.23：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过，renderer hash：`assets/index-CFXQ4Cwm.js`。
- `git diff --check` 通过；仅提示 Windows LF/CRLF warning。
