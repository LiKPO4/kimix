# Kimix v2.11.65

- 增强 Kimix 自管 Kimi Server 进程保护：foreground server 退出后不再保持“已连接”状态，后续恢复逻辑可重新拉起服务。
- 增强 Kimi Server WebSocket 断连处理：连续重连失败会触发运行时失效路径，避免长时间停留在不可用连接上。
- 保持官方 `kimi web` 入口不变；它仍由 Kimi Code CLI 自身托管。
