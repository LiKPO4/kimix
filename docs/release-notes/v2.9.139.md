# Kimix v2.9.139

- 修复运行 Write/Edit 等大参数工具时，过程卡片直接渲染完整 JSON/content 导致页面卡顿、计时器停顿的问题。
- 工具过程详情优先展示结构化参数，并对 `content`、`old_string`、`new_string` 等大字段做行数/字数摘要和短预览。
- 事件合并时不再把完全相同的完整 `rawArguments` 重复拼接，减少运行中状态的无效大文本。
