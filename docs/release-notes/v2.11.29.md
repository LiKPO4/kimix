# Kimix v2.11.29

## 修复

- 加载 Kimi Code 历史正文时优先读取官方 Server snapshot，并将官方历史消息转换为 Kimix 现有会话事件。
- Server snapshot 不可用时自动回落本地历史镜像，避免离线或 SDK fallback 会话无法打开。
- 官方 snapshot 历史中的用户消息也会还原为普通用户消息，避免只恢复助手正文。

