# Kimix v2.15.25

## 多 Agent 审批与提问所有权

- 房间中的工具审批和结构化提问现在严格使用事件所属 Agent 的 runtime，不再默认发送到 primary Agent。
- 审批或回答完成后只更新所属 Agent 的事件分区，不会改写其他 Agent 的历史状态。
- 房间事件缺少 owner、Agent 已移出/归档或 runtime 尚未就绪时会明确提示并阻止操作，避免静默串线。
- 审批卡和提问卡显示来源 Agent，便于并行工作时确认当前交互对象。
