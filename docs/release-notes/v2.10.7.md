# Kimix v2.10.7

## 启动恢复

- Server 会话已失效时不再在启动后显示“Session was not found”错误卡，自动创建 fresh runtime 并保留本地会话历史。
- 清理本地历史中已经落盘的同类启动恢复错误，避免旧红卡在后续启动时反复出现。
- fresh runtime 遇到瞬时 missing-session 时交由后台预热继续恢复，不打断用户打开应用。
