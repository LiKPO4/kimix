# Kimix v2.13.6

## 修复

- 修正 v2.13.5 对 Kimi Code 0.22 SDK 公开归档能力的误判：公开 Harness 实际未暴露内部 `archiveSession`。
- SDK fallback 现在会先由 Harness 确认官方会话目录，关闭活跃会话后按官方 SessionStore 相同语义写入 `archived: true`。
- 归档不删除会话目录或历史，必要时仍可通过官方数据恢复。
