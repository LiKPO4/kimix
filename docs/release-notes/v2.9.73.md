# v2.9.73

- 修复从 Kimi Code 历史恢复老会话时，图片输入只显示为“图片 N / 未读取到绝对路径”的问题。
- 历史解析同时兼容 `imageUrl` 和 `image_url` 两种图片字段，避免官方历史里仍有 dataUrl 但 Kimix 没读出来。
- 启动历史修复会识别已落盘的图片占位会话，并在官方历史可提供图片数据时自动补回。
- 补充历史图片回放测试，防止后续回归。
