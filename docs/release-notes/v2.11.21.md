# Kimix v2.11.21

- 修复直接发送 `/skill:` 时绕过新安装 Agent Skill 同步，导致父 Skill 可用但 `game-development/game-design` 等子 Skill 仍不在当前会话清单的问题。
- 新增展平子 Skill 后会立即通过官方 fork 刷新会话注册表，不再因安装包文件时间较旧而误判为无需刷新。
- 修复旧会话已有本地缓存时跳过官方历史映射，完整 `<kimi-skill-loaded>` 指令仍显示在用户气泡中的问题。
- 启动恢复时会迁移旧缓存：用户触发仅保留简短 `/skill:<name> [参数]`，模型触发转为 Skill 状态，同时清理 `User activated the skill...` 英文标题。
