# Kimix v2.15.37

## 多 Agent 房间启动恢复

- preload 会保留主进程最新 bootstrap 载荷，React 即使在 `did-finish-load` 之后才订阅也不会漏掉一次性启动上下文。
- 启动选择活动会话前等待本地会话完成水合，避免 bootstrap 先到时对空 store 选错最近项目或新会话。
- 上次活动会话是多 Agent 房间时，现在会先恢复该房间身份，再按 Agent 分区加载官方历史和 runtime 状态。
- 本地会话读取失败也会释放启动门禁，避免界面永久停留在等待状态。
