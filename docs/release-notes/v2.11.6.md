# Kimix v2.11.6

- 优化启动脚本：默认双击 `start-kimix.bat` 直接运行已构建的 Electron 包，避免 Vite dev 首次编译 renderer 造成约 20 秒白屏。
- 保留开发和强制冷启动入口：需要热更新时使用 `start-kimix.bat --dev`，需要排除旧进程、Vite 缓存或构建缓存时使用 `start-kimix.bat --clean`。
- 补充启动时序日志，区分主进程窗口创建、renderer 首帧、Kimi Server 后台启动与就绪耗时。
