# Kimix v2.9.34

## 修复

- 调整主对话流滚动条位置，减少消息区域右侧预留空白，让滚动条更靠近右边界。
- 保留稳定滚动槽，避免滚动条出现/消失重新引发布局抖动。
- 版本号三处同步到 v2.9.34：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
