# Kimix v2.9.154

## 优化

- 普通发送和空态建议发送新增 Kimi Code 链路状态提示。
- 发送后会显示当前卡在“准备链路、恢复旧 runtime、创建 runtime、runtime 失效重建、已提交给 Kimi Code”等阶段。
- 目标是区分前置连接耗时和真正模型思考耗时，避免用户只看到长时间“思考中”。

## 说明

- 本版复用现有状态事件展示，不新增复杂 UI。
- 目前显示的是 Kimix 前置链路阶段；后续可继续接入主进程实际 `server / sdk fallback` 路由标签。
