# Kimix v2.9.149

## 修复

- 修复开启 Kimi Code Server 会话路由后，本地 Server 短暂不可达或请求失败会直接在对话里显示 `fetch failed` 的问题。
- Server 新建、恢复或发送首轮消息失败时，会标记为 SDK fallback，并继续使用现有 SDK 主链路，避免实验开关打断正常对话。

## 说明

- Server 路由仍保持实验能力；失败原因会保留在 Server 状态里，便于后续排查。
