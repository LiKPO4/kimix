# Kimix v2.14.31

## 改进

- Kimi Web 风格过程区的 Swarm 子代理行支持展开查看子事件。
- 子事件按紧凑时间线展示，工具调用与工具结果会合并为一条摘要，避免在大量子代理场景下出现多层嵌套卡片。
- 单个子代理默认只展示最近 8 条子事件，必要时可展开全部。

## 边界

- 展开状态仅保存在当前界面，不写入会话历史，也不会影响会话排序。
- 子事件详情当前以摘要为主，不展示完整 raw payload。
