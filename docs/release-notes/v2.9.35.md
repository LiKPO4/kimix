# Kimix v2.9.35

## 修复

- 修复画板主体高度过高导致画布顶穿底部操作区的问题。
- 右侧画布区域改为固定容器内自适应居中，避免 1:1 画布按自身尺寸撑开弹窗。
- 版本号三处同步到 v2.9.35：`package.json`、侧栏、设置页。

## 验证

- `pnpm build` 通过。
- `git diff --check` 通过，仅有 LF/CRLF 提示。
