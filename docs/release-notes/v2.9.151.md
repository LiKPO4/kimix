# Kimix v2.9.151

## 优化

- 优化 Kimi Code 普通发送前置握手：当前会话已有 runtime id 时直接复用，不再每条消息前都 `resumeSession`。
- Kimi Code Server REST 控制请求增加 5 秒超时，Server create/prompt 等控制接口卡住时会更快 fallback 到 SDK，避免实验路由拖慢发送。

## 说明

- 如果 runtime id 已失效，发送失败后仍会清理并重建一次运行时，保持原有兜底能力。
