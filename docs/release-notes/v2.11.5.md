# Kimix v2.11.5

## 启动与权限修复

- 修复 Server 会话在“完全访问权限”下仍显示工具审批卡的问题：Kimix 的三档权限继续使用官方 `manual` / `auto` / `yolo`，其中 `yolo` 收到 Server approval 时会自动按会话批准。
- 启动时先显示本地项目和会话，再后台恢复官方 Kimi Code runtime 与历史，避免官方会话恢复拖慢首屏。
- 新增 renderer 启动阶段日志 `[KimixStartup]`，用于定位白屏发生在加载、React 首帧还是后台恢复阶段。
