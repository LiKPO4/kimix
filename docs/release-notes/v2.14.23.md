# Kimix v2.14.23

## 修复

- 继续修复 Skill 刷新 fork 导致的同名会话：当官方目录只返回 `skill-*` leaf、父会话只在 Kimix 本地镜像中时，也会折叠成同一个会话。
- 修复点击这类会话后一直显示“正在同步最新会话”：`skill-*` 历史为空时会回退读取父会话历史，已有缓存会话也会清理 stale loading 状态。

## 验收建议

- Project06 下同名“ 不对，我是说之前...”应在首屏就只显示一个。
- 点击该会话应能正常显示历史内容，不再卡在同步提示。
