# Kimix v2.9.92

## 修复

- 启动时只从主进程读取会话摘要，不再把完整大型会话历史传到 renderer，降低恢复后启动卡顿。
- 点开摘要会话时再按 id 拉取完整会话事件，旧会话仍可正常打开。
- 切换当前会话只保存本地活动上下文，不再写入大型主状态文件。
- 普通会话自动保存延后到更接近 5 秒兜底，减少进入会话约 1 秒后的可见停顿。

## 验证

- `git diff --check`
- `pnpm build`
