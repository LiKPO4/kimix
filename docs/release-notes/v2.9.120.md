# Kimix v2.9.120

## 修复
- 统一 Markdown 代码块外壳样式，避免列表内代码块和普通代码块出现左侧宽度不一致。
- 覆盖 highlight.js 默认 `pre code.hljs` / `code.hljs` 内边距，让 bash、dart、json 等语言代码块统一使用 Kimix 自己的代码块 padding。

## 验证
- `pnpm test:run -- src/utils/__tests__/assistantParagraphs.test.ts`
- `pnpm build`
